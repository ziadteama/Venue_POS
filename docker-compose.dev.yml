services:
  api:
    build:
      context: .
      dockerfile: docker/Dockerfile.api
      target: dev
    environment:
      NODE_ENV: development
    volumes:
      - ./apps/api:/app/apps/api
      - ./packages:/app/packages
    command: npm run dev -w @venue-pos/api
