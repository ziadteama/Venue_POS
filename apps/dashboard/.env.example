# Dev: leave unset — Vite proxies /api and /socket.io to :3000 (localhost + phone on LAN).
# Production build: set to your API origin, e.g. https://api.example.com
# VITE_API_URL=http://localhost:3000
